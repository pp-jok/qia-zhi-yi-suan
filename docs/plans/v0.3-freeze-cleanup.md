# v0.3 Freeze Cleanup Proposal

Do not implement before a separate compatibility review.

1. Hide the internal assurance parameter behind a qualified-facts public builder while retaining existing test seams.
2. Add lifecycle contract regressions for normalized, failed qualification, comparison failure, and caller-promotion refusal.
3. Add a release gate that creates tags only after CI and wheel verification are green.
4. Publish a v0.3.9 supersession note only; do not change semantic assets, profile output, or fingerprints.
